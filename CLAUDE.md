# CLAUDE.md

このファイルは、gameEditor の開発時に AI アシスタントが必ず参照する作業ガイドです。
仕様の一次情報は `specs.txt` です。仕様に迷った場合は `specs.txt` を優先し、このファイルは開発方針と実装判断の補助として扱ってください。

## プロジェクト概要

gameEditor は、ゲーム制作で使う各種データを作成するための Windows 向け 2D エディタです。
出力データは基本的に JSON とし、特定のゲームエンジンや独自形式に依存しない汎用的なデータを作成できることを目的とします。

現状の対象は原則 2D のみです。

## 実装対象

gameEditor は以下のエディタ群を内包します。
各エディタは他のエディタと干渉せず、独立して操作できる設計を目指します。

### マップエディター

最優先で実装する機能です。

- RPG で使うような、ブロック単位のマップチップを並べてマップを作成するエディタ
- RPGツクールや WOLF RPGエディターのような操作感を目指す
- 任意数のレイヤー、またはレベルを持てる
- 表示優先度や当たり判定を柔軟に設定できる
- 特定のチップに任意の ID を埋め込める
- 埋め込んだ ID はゲーム内イベント判定などに利用できる
- bmp、jpg など主要な画像フォーマットに対応する

### アニメーションエディター

マップエディターがある程度形になった後に実装します。

- キャラクターなどのパターンアニメを作成できる
- 拡大、縮小、回転などをサポートした 2D アニメーションを扱う
- 詳細仕様は今後検討

### エフェクトエディター

詳細検討中の機能です。

- パーティクルなどを使ったエフェクトを作成する
- アニメーションとは別機能として実装する方針
- 任意のアニメーションと結合できるようにする
- 将来的にアニメーションエディターへ統合する可能性がある

### コリジョンエディター

詳細検討中の機能です。

- 各アニメーションパターンに当たり判定を付与する
- 当たり判定は任意の矩形と円形を扱う
- 将来的にアニメーションエディターへ統合する可能性がある

## 技術方針

- 対応 OS は Windows
- 使用言語は C#
- Windows SDI アプリとして実装する
- 複数プロセス起動を可能にする
- Visual Studio でも make でも動作するようにする
- bmp などの画像描画には OpenGL を使用する
- 画像描画は高速で、操作中に引っかかりがないことを重視する
- 保存データ、エクスポートデータは原則 JSON を基本形式にする

## 実装優先順位

1. アプリケーションの外形を作る
2. マップエディターを作る
3. マップエディターが実用に近づいたら、アニメーションエディターでキャラクターのパターンアニメを実装する
4. エフェクトエディター、コリジョンエディターは詳細仕様を固めながら実装する

## 開発時の判断基準

- まずは仕様に沿った最小の動作する形を優先する
- マップエディター以外の機能は、現段階では過度に作り込まない
- エディタ同士のデータ構造は将来連携できるようにしつつ、操作や画面は独立性を保つ
- JSON の構造は、外部ゲームエンジンから読みやすいことを重視する
- 特定エンジン専用の概念を中核データ構造に入れすぎない
- UI は Windows デスクトップアプリとして自然な操作感を優先する
- 描画、スクロール、ズーム、チップ配置などの基本操作は軽快さを重視する

## 未確定事項

以下は `specs.txt` 時点で詳細検討中、または今後決めるべき事項です。

- アニメーションエディターの詳細 UI とデータ形式
- エフェクトエディターを独立させるか、アニメーションエディターに含めるか
- コリジョンエディターを独立させるか、アニメーションエディターに含めるか
- マップのレイヤー、レベル、表示優先度、当たり判定の具体的な JSON スキーマ
- チップ ID の用途、型、命名規則
- 対応する画像フォーマットの正確な範囲
- OpenGL の利用ライブラリ、ウィンドウフレームワーク、ビルド構成

## AI アシスタント向け作業ルール

- 作業前にこの `CLAUDE.md` と `specs.txt` を確認する
- `specs.txt` とこのファイルが矛盾する場合は `specs.txt` を優先する
- 実装が始まった後は、既存コードの構成と命名を優先する
- ユーザーが明示していない大きな設計変更は、実装前に意図を確認する
- 仕様未確定の箇所は、仮実装であることが分かるように小さく閉じる
- マップエディターの進捗を最優先に考える
- 将来のアニメーション、エフェクト、コリジョン機能を妨げる密結合は避ける
- 日本語の仕様、コメント、ドキュメントは文字化けに注意して UTF-8 で扱う

